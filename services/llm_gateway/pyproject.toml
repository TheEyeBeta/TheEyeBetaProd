[project]
name = "llm-gateway"
version = "0.1.0"
description = "LiteLLM proxy (Docker); provisioning scripts only in-repo"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "litellm[proxy]>=1.88.1",
]

[tool.uv]
package = false
