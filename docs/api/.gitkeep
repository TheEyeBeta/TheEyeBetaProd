# docs/api placeholder
# main.openapi.json is not generated here because the main API service
# (port 7000) lives in the sibling TheEyeBetaDataAPI repository.
# See docs/build-log.md §17.25 and Lessons Learned §2.
# oms.openapi.json and admin.openapi.json are generated by `make docs-api`.
